language: java

jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7
  - openjdk6

before_install:

install: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V -Djava.version=1.6

script: mvn clean verify -P shade -P sources -P javadoc -Djava.version=1.6

after_success:
after_failure:
